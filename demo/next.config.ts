import type { NextConfig } from "next";

// Next.js 的配置文件。
// 本项目刻意保持"零配置"——教程第一章、第二章讲的路由与布局，
// 全部只依赖文件系统约定，不需要在这里注册任何东西。
const nextConfig: NextConfig = {
  /* 需要时在这里添加配置项，例如 redirects、images、experimental 等 */
};

export default nextConfig;